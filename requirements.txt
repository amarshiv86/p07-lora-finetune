# Training dependencies (run on GPU machine — Colab/Kaggle free tier works)
torch>=2.2.0
transformers>=4.40.0
peft>=0.10.0
datasets>=2.18.0
accelerate>=0.27.0
bitsandbytes>=0.43.0     # QLoRA 4-bit quantization
rouge-score>=0.1.2       # Evaluation metrics
huggingface-hub>=0.23.0  # Push adapter to HF Hub

# Dev / test (no GPU needed)
pytest>=8.0.0
pytest-asyncio>=0.23.0
ruff>=0.4.0
